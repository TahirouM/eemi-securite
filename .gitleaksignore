# Faux positif justifie : la cle d'exemple presente dans CLAUDE.md est un
# EXEMPLE pedagogique du sujet de TP (et NON un vrai secret). On l'ignore
# explicitement pour que la branche passe le gate Gitleaks.
# Le vrai secret hardcode reste detecte sur la branche `vulnerable` (src/app.js).
#
# Fingerprint en mode scan filesystem (--no-git) :
CLAUDE.md:stripe-access-token:239
